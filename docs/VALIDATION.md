# 验证记录

日期：2026-09-08。此处记录工程验证，不等于用户验证或商业效果验证。

| 检查 | 结果 |
| --- | --- |
| 业务测试 | 8 / 8 通过：指标分母、零分母、无效输入、同口径开关、JSON 完整性、文案漏项、人工复核保留、简报引用 |
| TypeScript | `tsc --noEmit` 通过 |
| 产品代码静态检查 | `oxlint app lib tests vite.static.config.ts` 通过 |
| 静态生产构建 | `pnpm build:static` 通过 |
| 生产资源请求 | 本地静态预览首页、JS、CSS、favicon 和调研下载均返回 HTTP 200 |
| 开发页面 | 本地首页返回 HTTP 200，包含项目标题和业务内容 |

## 已检查的关键行为

- 支付买家与订单使用不同口径；零分母返回缺失值，不伪装成零表现。
- 缺少成本字段的 JSON 不按零处理；退款多于订单、买家多于访客、非有限金额会报错。
- 没有确认口径时不计算转化差异；确认后仅呈现观察差异。
- 草稿补齐字段后仍然保留人工复核提醒，导出包含来源与待验证项。

## 尚未完成的验证

- 尚未开展真实运营人员任务试验、商家访谈或商业投放。
- 未做浏览器交互自动化、屏幕阅读器或多设备验收；HTTP 成功不等于所有交互已经实测。
- 可选 WebMCP 只读简报工具已加入支持检测和清理；当前环境无可用的 WebMCP 调用上下文，未宣称端到端验证。
- GitHub Actions 与 GitHub Pages 的线上运行结果应以仓库实际记录为准。
- 未测试服务端 `pnpm build` 路径；实际交付使用静态生产构建。

## 数据责任

公开资料只支持本轮提出假设；内部示例数据全部为模拟。字段检查只检测有限规则和是否填写，不判断来源是否真实，也不替代法务或平台审核。
